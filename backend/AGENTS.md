# AGENTS.md

Single source of truth for every coding agent (Codex, Claude Code, others):
**[`CLAUDE.md`](CLAUDE.md)** in this directory. Read it in full before touching
any code.

It covers the spec-routing (`docs/index.md`), requirement-ID rules, build /
test / lint commands, the ten-worker architecture, structural rules, the
fund-safety invariants, and the build order. Root context:
[`../CLAUDE.md`](../CLAUDE.md).
