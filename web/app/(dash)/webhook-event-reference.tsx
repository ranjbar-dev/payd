export function WebhookEventReference() {
  return <section className="border border-border-subtle bg-panel p-4"><h2 className="text-lg font-semibold">Event reference</h2><p className="mt-1 text-sm text-ink-secondary">Use this reference to confirm whether a missing notification is expected to exist.</p><div className="mt-3 grid gap-4 lg:grid-cols-2"><div><h3 className="font-medium">Order-scoped — exactly one consumer</h3><p className="mt-1 text-xs text-ink-secondary">The order’s consumer, or the configured default, receives each event.</p><ul className="mt-2 space-y-1 font-mono text-sm"><li>order.payment_seen</li><li>order.partial</li><li>order.paid</li><li>order.confirmed</li><li>order.expired</li><li>order.reverted</li></ul></div><div><h3 className="font-medium">Global — every receives_global consumer</h3><ul className="mt-2 space-y-1 font-mono text-sm"><li>payment.unattributed</li><li>withdrawal.broadcast</li><li>withdrawal.confirmed</li><li>withdrawal.failed</li><li>withdrawal.needs_operator</li><li>energy.purchase_failed</li><li>energy.balance_low</li><li>balance.drift_detected</li></ul></div></div><div className="mt-4 border-t border-border-subtle pt-4"><h3 className="font-medium">Delivery verification</h3><p className="mt-2 text-sm text-ink-secondary">Consumers verify <code className="font-mono">X-Event-Id</code>, <code className="font-mono">X-Timestamp</code>, <code className="font-mono">X-Consumer</code>, and <code className="font-mono">X-Signature</code>.</p><p className="mt-2 text-sm text-ink-secondary">Signature: <code className="font-mono">hex(HMAC-SHA256(secret, timestamp + "." + raw_body))</code>, computed with that consumer’s own secret. Secrets are never returned to or displayed by this dashboard.</p></div></section>;
}
