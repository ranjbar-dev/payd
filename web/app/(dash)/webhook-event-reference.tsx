export function WebhookEventReference() {
  return <section className="card space-y-3" aria-labelledby="event-reference-heading"><div><h2 id="event-reference-heading" className="card-title">Event reference</h2><p className="mt-1 text-[13px] text-ink-secondary">Use this reference to confirm whether a missing notification is expected to exist.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[42rem] border-collapse text-left text-[13px]"><caption className="sr-only">Webhook event delivery reference</caption><thead><tr><th className="th">Scope</th><th className="th">Fan-out</th><th className="th">Event types</th></tr></thead><tbody><tr className="row-hover"><td className="td font-medium">Order-scoped</td><td className="td text-ink-secondary">Exactly one: the order’s consumer, or the configured default.</td><td className="td font-mono text-[12px] leading-5">order.payment_seen · order.partial · order.paid · order.confirmed · order.expired · order.reverted</td></tr><tr className="row-hover"><td className="td font-medium">Global</td><td className="td text-ink-secondary">Every consumer with <code className="font-mono">receives_global</code>.</td><td className="td font-mono text-[12px] leading-5">payment.unattributed · withdrawal.broadcast · withdrawal.confirmed · withdrawal.failed · withdrawal.needs_operator · energy.purchase_failed · energy.balance_low · balance.drift_detected</td></tr></tbody></table></div><div className="border-t border-border-subtle pt-3"><h3 className="card-title">Delivery verification</h3><dl className="mt-2 grid gap-x-6 gap-y-2 text-[13px] md:grid-cols-[auto_1fr]"><dt className="text-[11px] uppercase text-ink-faint">Headers</dt><dd className="font-mono text-[12px] text-ink-secondary">X-Event-Id · X-Timestamp · X-Consumer · X-Signature</dd><dt className="text-[11px] uppercase text-ink-faint">Signature</dt><dd className="font-mono text-[12px] text-ink-secondary">hex(HMAC-SHA256(secret, timestamp + "." + raw_body))</dd></dl><p className="mt-2 text-[11px] text-ink-secondary">Computed with that consumer’s own secret. Secrets are never returned to or displayed by this dashboard.</p></div></section>;
}
